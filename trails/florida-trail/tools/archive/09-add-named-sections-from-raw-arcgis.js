const fs=require('fs');
const turf=require('@turf/turf');
const base='trails/florida-trail/data/';
const master=JSON.parse(fs.readFileSync(base+'points_all_merged.json','utf8')); master.points=master.points||[];
const gj=JSON.parse(fs.readFileSync(base+'raw-ft-arcgis.geojson','utf8'));
const feats=(gj.features||[]).filter(f=>f&&f.properties&&f.geometry&&(f.geometry.type==='LineString'||f.geometry.type==='MultiLineString'));
const names=['CR 523','Deen Still Rd','CR 314','Cross Florida Greenway East','Cross Florida Greenway West','Cross Florida Greenway Dunnellon','CFG Dunnellon Bridge','Shangri La Spur Trail','Baseline Connector','Hulls Creek Swamp','Old Canoe Creek Road','Bill Johnston Memorial Parkway','Ronald Reagan Parkway','John Young Parkway','SR 471','Colonel Robins Campsite Spur','Citrus','Landbridge','Withlacoochee State Trail','Croom'];
const tn=f=>String(f.properties.Trail_Name||'');
const cor=f=>String(f.properties.Corridor||'');
const rk=f=>String(f.properties.FNST_Rank||'').toUpperCase();
const des=f=>String(f.properties.Designatio||'');
const isWanted=(f)=>{const r=rk(f);const d=des(f);return r==='OPPORTUNITY'||r==='SPUR TRAIL'||d==='Connector'||d==='Side Trail'||d==='Spur Trail';};
const existing=new Set(master.points.map(p=>p.id));
const step=1,idScale=1000;
const q=(pt)=>{const k=1e5;return (Math.round(pt[0]*k)/k)+','+(Math.round(pt[1]*k)/k);};
const buildBackbone=(features)=>{let segs=[];for(const f of features){const g=f.geometry;if(g.type==='LineString') segs.push(g.coordinates); else for(const c of (g.coordinates||[])) segs.push(c);} segs=segs.filter(c=>Array.isArray(c)&&c.length>=2); const nodes=new Map(); const addNode=(k,pt)=>{if(!nodes.has(k)) nodes.set(k,{k,pt,edges:[]}); return nodes.get(k);}; const edges=[]; for(const coords of segs){const a=coords[0], b=coords[coords.length-1]; const ka=q(a), kb=q(b); const na=addNode(ka,a), nb=addNode(kb,b); const fLine={type:'Feature',properties:{},geometry:{type:'LineString',coordinates:coords}}; const w=turf.length(fLine,{units:'miles'}); const e={a:ka,b:kb,w,coords}; edges.push(e); na.edges.push(e); nb.edges.push(e);} const keys=[...nodes.keys()]; if(keys.length<2) throw new Error('Not enough nodes'); const comp=new Map(); let cid=0; for(const k of keys){ if(comp.has(k)) continue; cid++; const stack=[k]; comp.set(k,cid); while(stack.length){const u=stack.pop(); for(const e of nodes.get(u).edges){const v=(e.a===u?e.b:e.a); if(!comp.has(v)){comp.set(v,cid); stack.push(v);} } } } const compW=new Map(); for(const e of edges){const c=comp.get(e.a); compW.set(c,(compW.get(c)||0)+e.w);} let bestCid=null,bestW=-1; for(const [c,w] of compW){ if(w>bestW){bestW=w;bestCid=c;}} const keepNode=(k)=>comp.get(k)===bestCid; const keepEdge=(e)=>keepNode(e.a)&&keepNode(e.b); const subKeys=keys.filter(keepNode); const dijkstra=(start)=>{const dist=new Map();const prev=new Map();const seen=new Set();for(const k of subKeys) dist.set(k,Infinity); dist.set(start,0); while(true){let u=null,best=Infinity; for(const [k,v] of dist){ if(!seen.has(k)&&v<best){best=v;u=k;}} if(u===null) break; seen.add(u); const nu=nodes.get(u); for(const e of nu.edges){ if(!keepEdge(e)) continue; const v=(e.a===u?e.b:e.a); const nd=best+e.w; if(nd<dist.get(v)){dist.set(v,nd); prev.set(v,{u,e});}} } return {dist,prev};}; const pickFarthest=(start)=>{const {dist}=dijkstra(start); let fk=start,fd=-1; for(const [k,v] of dist){ if(v<Infinity && v>fd){fd=v;fk=k;}} return {fk,fd};}; const A=pickFarthest(subKeys[0]).fk; const B=pickFarthest(A).fk; const {prev}=dijkstra(A); if(!prev.has(B)) throw new Error('No path found in largest component'); const pathEdges=[]; let cur=B; while(cur!==A){const step=prev.get(cur); pathEdges.push(step.e); cur=step.u;} pathEdges.reverse(); let stitched=[]; let cursor=null; for(const e of pathEdges){let c=e.coords; const ca=q(c[0]), cb=q(c[c.length-1]); if(cursor===null){stitched=stitched.concat(c); cursor=q(stitched[stitched.length-1]); continue;} if(ca===cursor){stitched=stitched.concat(c.slice(1));} else if(cb===cursor){stitched=stitched.concat(c.slice(0,-1).reverse());} else {const last=stitched[stitched.length-1]; const d0=turf.distance(turf.point(last),turf.point(c[0]),{units:'miles'}); const d1=turf.distance(turf.point(last),turf.point(c[c.length-1]),{units:'miles'}); if(d0<=d1) stitched=stitched.concat(c.slice(1)); else stitched=stitched.concat(c.slice(0,-1).reverse());} cursor=q(stitched[stitched.length-1]); } const backbone={type:'Feature',properties:{component_nodes:subKeys.length,component_edges:edges.filter(keepEdge).length},geometry:{type:'LineString',coordinates:stitched}}; const L=turf.length(backbone,{units:'miles'}); return {backbone,L};};
const slug=(s)=>s.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'');
let addedTotal=0;
for(const nm of names){const sel=feats.filter(f=>isWanted(f)&&tn(f).toLowerCase().includes(nm.toLowerCase())); if(!sel.length){console.log('SKIP',nm,'(0 wanted features)'); continue;} const {backbone,L}=buildBackbone(sel); const ab=slug(nm); fs.writeFileSync(base+`trail_named_${ab}_backbone.geojson`,JSON.stringify(backbone)); let added=0,total=0; for(let m=0;m<=L+1e-9;m+=step){const p=turf.along(backbone,m,{units:'miles'}); const miInt=Math.round(m*idScale); const id=`ft-named-${ab}-mi${String(miInt).padStart(7,'0')}`; total++; if(existing.has(id)) continue; master.points.push({route:`named-${ab}`,needs_review:true,axis_mile:Math.round(m),mile:Math.round(m),id,lat:p.geometry.coordinates[1],lon:p.geometry.coordinates[0],corridor:cor(sel[0])||'',segment_code:'',section_code:'',segment_name:'',section_name:nm,include:true,notes:'}); existing.add(id); added++; } addedTotal+=added; console.log('OK',nm,'features',sel.length,'rank',rk(sel[0]),'desig',des(sel[0]),'miles',L.toFixed(2),'points',total,'new',added); }
master.meta=master.meta||{}; master.meta.plus_named_sections_on=new Date().toISOString().slice(0,10); master.meta.plus_named_sections_added=addedTotal;
fs.writeFileSync(base+'points_all_plus_namedsections.json',JSON.stringify(master,null,2));
const header=['route','needs_review','mile','id','lat','lon','corridor','segment_code','section_code','segment_name','section_name','include','notes'];
const esc=(v)=>{let s=String(v??'');s=s.split('\\r').join('');s=s.split('\\n').join(' ');s=s.split('\t').join(' ');return s;};
let out=header.join('\t')+'\n';for(const p of master.points){out+=header.map(h=>esc(h==='needs_review'?(p.needs_review?1:0):p[h])).join('\t')+'\n';} fs.writeFileSync(base+'points_all_plus_namedsections_review.tsv',out);
console.log('WROTE points_all_plus_namedsections_review.tsv rows',master.points.length,'(filter needs_review=1)' );
